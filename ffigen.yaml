# Run with `dart run ffigen --config ffigen.yaml`.
name: GradesNativeFibonacciBindings
description: |
  Bindings for `src/grades_native_fibonacci.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/grades_native_fibonacci_bindings_generated.dart'
headers:
  entry-points:
    - 'src/grades_native_fibonacci.h'
  include-directives:
    - 'src/grades_native_fibonacci.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
